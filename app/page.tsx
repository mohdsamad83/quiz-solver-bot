export default function HomePage() {
  return (
    <main style={{ fontFamily: 'sans-serif', textAlign: 'center', padding: '2rem' }}>
      <h1>Data Quiz Solver API</h1>
      <p>This is a Next.js application that provides an API to solve data quizzes automatically.</p>
      <p>The API endpoint is available at <code>/api/quiz</code> and accepts POST requests.</p>
    </main>
  );
}
